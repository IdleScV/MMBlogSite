
<div class="d-flex flex-row p-5 container">
    <div class=" card" style="width: 18rem; height: 32rem;">
        <img class="card-img-top" src="<%=Faker::Avatar.image%>" alt="Card image cap">

        <ul class="list-group list-group-flush">
            <li class="list-group-item d-flex justify-content-between">
                <div class="font-weight-bold">USERNAME: </div> 
                <div class="font-weight-light"><%=@user.username%></div>
            </li>
            <li class="list-group-item d-flex justify-content-between">
                <div class="font-weight-bold">NAME: </div> 
                <div class="font-weight-light"><%=@user.name%></div>
            </li>
            <li class="list-group-item d-flex justify-content-between">
                <div class="font-weight-bold">EMAIL: </div> 
                <div class="font-weight-light"><%=@user.email%></div>
            </li>
        
        <% if current_user == @user %>
            <li class="list-group-item d-flex justify-content-between">
                <div><%= button_to "Edit Information", edit_user_path(@user), method: :get,class: "btn btn-secondary"%></div>
                <%= form_tag(user_path(@user), method: :delete) do %>
                    <div ><%= submit_tag "Delete", class: "btn btn-secondary"%></div>
                <%end%>
            </li>
        <% end %>
        </ul>
    </div>
    <div class="column">
        <div id="accordion">
        <% @user.blogs.each do |blog| %>
            <div class="card">
                <div class="card-header" id="heading<%=blog.id%>%">



                
            </div>
        <% end %>
        </div>
    </div>
<div>

